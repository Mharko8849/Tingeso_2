apiVersion: apps/v1
kind: StatefulSet

metadata:
  name: loans-db-prod

spec:
  serviceName: "loans-db-prod"

  replicas: 1
  
  selector:
    matchLabels:
      app: loans-db-prod
  
  template:
    metadata:
      labels:
        app: loans-db-prod
  
    spec:
      containers:
      - name: loans-db-prod 
        image: postgres:15-alpine
  
        ports:
        - containerPort: 5432
  
        env:
        - name: POSTGRES_DB
          value: loans_db_prod
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: global-config
              key: POSTGRES_USER

        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: POSTGRES_PASSWORD

        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data

        resources:
          requests:
            memory: "256Mi"
            cpu: "50m"
          limits: 
            memory: "768Mi"
            cpu: "1000m"

  volumeClaimTemplates:
  - metadata:
      name: postgres-storage

    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 1Gi

--- 

apiVersion: v1
kind: Service
metadata:
  name: loans-db-prod
spec:
  ports:
  - port: 5432
  selector:
    app: loans-db-prod
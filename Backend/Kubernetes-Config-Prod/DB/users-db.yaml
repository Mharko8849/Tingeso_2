apiVersion: apps/v1
kind: StatefulSet

metadata:
  name: users-db-prod

spec:
  serviceName: "users-db-prod"

  replicas: 1
  
  selector:
    matchLabels:
      app: users-db-prod
  
  template:
    metadata:
      labels:
        app: users-db-prod
  
    spec:
      containers:
      - name: users-db-prod 
        image: postgres:15-alpine
  
        ports:
        - containerPort: 5432
  
        env:
        - name: POSTGRES_DB
          value: users_db_prod
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: global-config
              key: POSTGRES_USER

        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: POSTGRES_PASSWORD

        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data

        resources:
          requests:
            memory: "256Mi"
            cpu: "50m"
          limits: 
            memory: "768Mi"
            cpu: "1000m"

  volumeClaimTemplates:
  - metadata:
      name: postgres-storage

    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 1Gi

--- 

apiVersion: v1
kind: Service
metadata:
  name: users-db-prod
spec:
  ports:
  - port: 5432
  selector:
    app: users-db-prod
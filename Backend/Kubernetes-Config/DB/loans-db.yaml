apiVersion: apps/v1
kind: StatefulSet

metadata:
  name: loans-db

spec:
  serviceName: "loans-db"

  replicas: 1
  
  selector:
    matchLabels:
      app: loans-db
  
  template:
    metadata:
      labels:
        app: loans-db
  
    spec:
      containers:
      - name: loans-db 
        image: postgres:15-alpine
  
        ports:
        - containerPort: 5432
  
        env:
        - name: POSTGRES_DB
          value: loans_db
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: global-config
              key: POSTGRES_USER

        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: POSTGRES_PASSWORD

        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data

        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits: 
            memory: "512Mi"

  volumeClaimTemplates:
  - metadata:
      name: postgres-storage

    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 1Gi

--- 

apiVersion: v1
kind: Service
metadata:
  name: loans-db
spec:
  ports:
  - port: 5432
  selector:
    app: loans-db